# Environment Configuration
ENVIRONMENT=development

# API Configuration - Use API Gateway for all requests (LOCALHOST)
API_GATEWAY_URL=http://localhost:3001/api

# Individual service URLs (for reference only - not used directly)
# All requests go through API Gateway which routes to these services:
# - FHIR API Backend: http://localhost:8080/api
# - Auth Service: http://localhost:3002/api
# - Visits Service: http://localhost:3008/api
# - S3 Bucket Service: http://localhost:3009/api

# AWS S3 Configuration (for direct uploads if needed)
AWS_REGION=us-east-1
S3_BUCKET_NAME=nursing-app-dev-bucket
# Note: For production, use IAM roles instead of hardcoded keys

# Audio Recording Settings
MAX_RECORDING_DURATION_SECONDS=300
AUDIO_FILE_PREFIX=audio_recordings/

# Photo Settings
MAX_PHOTO_SIZE_MB=5
PHOTO_QUALITY=90
MAX_PHOTO_RESOLUTION=1080
PHOTO_FILE_PREFIX=photos/

# App Settings
APP_NAME=Nurse Mobile App
APP_VERSION=1.0.0
ENABLE_DEBUG_LOGGING=true

# Timeouts (in seconds)
API_TIMEOUT_SECONDS=30
UPLOAD_TIMEOUT_SECONDS=60

# Demo Credentials
DEMO_EMAIL=anna.virtanen@nursinghome.fi
DEMO_PASSWORD=password123
